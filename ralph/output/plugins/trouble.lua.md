# trouble.lua - Plugin Keybindings

**Plugin:** folke/trouble.nvim
**Source:** `lua/plugins/trouble.lua`

## Custom Keybindings

*No custom keybindings defined in this file.*

The plugin is loaded with default options (`opts = {}`). See `plugin_defaults:trouble.nvim` for default keybindings provided by the plugin.

## Notes

- Plugin is loaded via command: `Trouble`
- Uses lazy loading (only loads when `:Trouble` command is invoked)
