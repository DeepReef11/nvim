# Plugin: ui/image.lua

## Plugins Configured
- **luarocks.nvim** (`vhyrro/luarocks.nvim`) - LuaRocks package manager for Neovim
- **image.nvim** (`3rd/image.nvim`) - Image viewing in Neovim (currently **disabled**)

## Custom Keybindings

**None defined** - This plugin configuration does not define any custom keybindings.

## Notes
- The image.nvim plugin is disabled (`enabled = false`)
- Uses luarocks.nvim as a dependency for the magick library
- No `keys` table or `vim.keymap.set()` calls present
