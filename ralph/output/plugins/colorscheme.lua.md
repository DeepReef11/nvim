# Colorscheme Plugin Keybindings

**Source:** `lua/plugins/ui/colorscheme.lua`

## Summary

This file configures colorscheme plugins (`tokyonight.nvim` and `onedark.nvim`). No keybindings are defined in this configuration.

## Keybindings

*No keybindings defined in this file.*

## Notes

- The `onedark.nvim` plugin has a `toggle_style_key` option set to `nil` (disabled)
- If enabled, `toggle_style_key` could be set to a keybinding like `<leader>ts` to cycle through theme styles
- Theme styles available: dark, darker, cool, deep, warm, warmer, light
